import GalleryFieldImpact from "@/components/gallery/GalleryFieldImpact";
import { galleryFieldImpactData } from "@/lib/data/galleryFieldImpactData";

export default function GalleryFieldImpactPage() {
  return <GalleryFieldImpact data={galleryFieldImpactData} />;
}
